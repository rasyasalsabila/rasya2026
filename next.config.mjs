/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/rasya2026", 
  assetPrefix: "/rasya2026",
};
export default nextConfig;
